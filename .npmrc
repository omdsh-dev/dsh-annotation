# The @deepseek-ai/* peer packages are private (never on the public registry).
# scripts/link-dsh.mjs symlinks them into node_modules from the local DSH
# checkout after every `pnpm install`.
auto-install-peers=false
strict-peer-dependencies=false
