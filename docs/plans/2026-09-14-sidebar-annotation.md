# 侧边栏文件批注

- [x] 核对 #56：仅面向低于当前支持下限的宿主，且未解决缺失会话依赖，不合入该兼容路径。
- [x] 复用检查：当前 DSH 0.1.5-rc.2 提供文档预览及稳定文件地址标记，未提供文件批注；dsh-external 仓库目录未找到等价功能，继续扩展本插件。
- [x] 接入文本、Markdown、代码预览的选区，来源地址约束到当前会话，正文与文件路径一起保存和发送。
- [x] 用文件地址区分同文不同文件；重开后仅在原文件重新定位，禁止落到会话同文。
- [x] 复用编辑、发送、存储和历史标签，验证刷新、切文件、同文去重及旧流程。
- [x] 构建、测试、隔离浏览器与真实宿主验收；整理可审查提交。

PDF、图片与 iframe 内 HTML 没有相同的正文选区接口，不声明覆盖这些格式。当前工作不发布版本或替换日常宿主。

## 验证结果

21 项测试、构建、语法检查、安装包校验通过。两个无界面浏览器检查覆盖旧浮层交互和新文件选区、来源路径、文件隔离、滚动裁剪、刷新恢复、编辑和拼稿。DSH 0.1.5-rc.2 隔离真实宿主通过工作区文件打开、选择、保存、刷新后重新打开及编辑验证；宿主刷新会关闭侧边栏，本插件不改变此行为。模型未配置凭据，不将本次验收声称为真实模型回复验证。#49/#50/#52 保持等待报告者原环境复测。
